# Product images

Drop product photos here and reference them from `data/products.json`,
e.g. `"image": "assets/products/barcelona-home.jpg"`.

If a product has no `image` (or the file is missing), the site
automatically falls back to a black/gray placeholder - no broken
image icons.
